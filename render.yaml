services:
  - type: web
    name: hexland-server
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
